syntax = "proto3";

package customer;
option java_package="com.derocode.customer";
option java_multiple_files=true;
option java_outer_classname="CustomerProto";

message CustomerRequest{
  string id=1;
}

message AddCustomerAddress {
  optional string houseNumber=1;
  optional string street=2;
  optional string city=3;
  optional string state=4;
  optional string zipCode=5;
}

message CustomerResponseAddress {
  optional string houseNumber=1;
  optional string street=2;
  optional string city=3;
  optional string state=4;
  optional string zipCode=5;
}

message CustomerResponse{
  optional string id=1;
  optional string firstName=2;
  optional string lastName=3;
  optional string email=4;
  repeated CustomerResponseAddress addresses=5;
}

message AddCustomerRequest{
  optional string firstName=1;
  optional string lastName=2;
  optional string email=3;
  repeated AddCustomerAddress addresses=4;
}

message CustomerResponseList{
  repeated CustomerResponse customerResponse=1;
}

message Empty{};


service CustomerService{

  rpc AddCustomer(AddCustomerRequest) returns (CustomerResponse);

  rpc GetCustomerById(CustomerRequest) returns (CustomerResponse);

  rpc GetAllCustomers(Empty) returns (CustomerResponseList);

  rpc DeleteCustomer(CustomerRequest) returns (Empty);

}